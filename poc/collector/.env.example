# Copia este archivo como `.env` (ignorado por git) y complétalo.
# Nunca subas `.env` ni su contenido al repositorio.
# Cárgalo con: set -a; source .env; set +a

TENANT_ALIAS=lab-interno

VSPHERE_HOST=https://vcenter.ejemplo.local
VSPHERE_USER=usuario_solo_lectura
VSPHERE_PASSWORD=
VSPHERE_VERIFY_SSL=true

XCLARITY_HOST=https://xclarity.ejemplo.local
XCLARITY_USER=usuario_solo_lectura
XCLARITY_PASSWORD=
XCLARITY_VERIFY_SSL=true
